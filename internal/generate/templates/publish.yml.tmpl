# Generated by releaser. Do not edit by hand — re-run `releaser generate`.
#
# This workflow publishes a release when the pending-release pull request
# is merged into the default branch. Idempotent: re-runs on a partially
# completed release only perform the steps that are still missing.

name: releaser publish

on:
  pull_request:
    types:
      - closed
    branches:
      - main

permissions:
  contents: write

jobs:
  publish:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
<<- range .SetupSteps >>
<< . | indent 6 >>
<<- end >>
      - name: Publish release
        uses: bombfork/releaser@<< .ActionRef >>
        with:
          command: release publish
          github-token: ${{ secrets.GITHUB_TOKEN }}
